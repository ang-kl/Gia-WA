# Journal — Gia-WA v0.1.0

> Per-PR append-only journal. Format mirrors `ang-kl/gia/doc/Journal/`.
> Serial numbers per CLAUDE-FULL.md §3; HDR blocks per §10.

(№ 1 - 15-05 '26 14:44 SGT)

[HDR] #001 | 14:44:00 SGT 15-05-26 | v0.1.0 | ~6kb | ~700 lines
[INTENT] Bootstrap `ang-kl/Gia-WA` per BUILD-PLAN.md Phases 0 + 1: scaffold repo, ship WhatsApp transport skeleton + markup escaper + webhook signature verifier, all green under vitest + node --check.
[DELTA]
- Phase 0: README.md (project pointer), LICENSE (MIT), .gitignore, .env.example (full var list per §9.1), package.json@0.1.0 (axios + express + ioredis + vitest; explicit non-deps: no @anthropic-ai/sdk per D-1).
- Phase 0: doc/Journal/, doc/decisions/, doc/.serial-state.yml (counters fresh at 0 per BUILD-PLAN §0.6; anchored 2026-05-15T14:44+08:00 from server HTTP Date header in the sandbox proxy — outbound to data.gov.sg + worldtimeapi blocked by allowlist, so the sovereign endpoint chain in TF-9 step 1 + step 2 was unavailable; HTTP Date header is the next-best server-rendered time per TF-10).
- Phase 0: .github/workflows/ci.yml (syntax + test jobs), publish-flows.yml (skeleton, gated on PUBLISH_FLOWS_ENABLED repo var).
- Phase 1: server.js (Express, three routes: GET /webhook verify-token echo, POST /webhook signature-verifying stub, POST /flows/data 503 stub, plus GET /health).
- Phase 1: src/transport/whatsapp.js (sendText with retry-on-429/5xx; other methods throw 'not implemented' stubs).
- Phase 1: src/transport/format.js (escapeMarkup via ZWSP sandwich, renderVenue, renderVenueList, chunkForWhatsApp).
- Phase 1: src/flows/sign.js (x-hub-signature-256 verify with timing-safe compare).
- Phase 1: __tests__/transport/format.test.js (14 cases), __tests__/flows/sign.test.js (7 cases), __tests__/server.test.js (6 cases).
- Decision ADR: doc/decisions/001-single-branch-per-session-deviation.md (records why all phases land on `claude/build-gia-wa-StD7x` instead of one-branch-per-phase per BUILD-PLAN §12).
[RISK] Low. No live integrations; no secrets touched; all behaviour is unit-tested or signature-gated. The Flows Data Channel returns 503 until Phase 4 ships, so Meta cannot publish a Flow against an unfinished endpoint by accident.
[STATUS] ALPHA — scaffold only, no AI integration, no Cloud API smoke test yet.
[TEST]
- npm run syntax → 0 errors across all .js
- npm test -- --run → 27 / 27 green (format 14, sign 7, server 6)
- GET /health, GET /webhook (correct + wrong tokens), POST /webhook (signed + unsigned), POST /flows/data (signed) all asserted end-to-end via supertest-equivalent fetch against a live http.Server in the test process.
[NEXT] Operator review of Phase 0 + Phase 1 commit; decide on Phase 2 entry (Llama wrapper requires GROQ_API_KEY + TOGETHER_API_KEY provisioning). Confirm §14 open questions before Phase 4: vault bridge (Option B submodule default), Tavily-vs-Serper, cross-transport identity linking out of scope.
[ADVICE] Keep the Phase 1 / Phase 2 boundary firm. Phase 2 adds real provider dependencies + per-call cost telemetry; operator should sign off on provider keys before that branch opens. Avoid retrofitting Llama into the Phase 1 PR — that's exactly how scope creep eats a transport refactor.

---

(№ 2 - 22-05 '26 05:05 SGT)

[HDR] #002 | 05:05:00 SGT 22-05-26 | v0.1.0 | ~9kb | ~200 lines
[INTENT] Bring the doc/ system into structural conformance with the CLAUDE-FULL.md §1 contract, and add a repo-root CLAUDE.md so future Claude Code sessions onboard from a single orientation file.
[DELTA]
- CLAUDE.md (repo root): created via /init — common commands, architecture (three-route Express server, raw-body signature verification, the src/ vs top-level-scaffold layout tension), the D-1…D-4 non-negotiables, and WhatsApp-specific gotchas (markup escaping, Flow flipped-IV crypto, wa: Redis keyspace).
- doc/ scaffold: created the 8 CLAUDE-FULL.md §1 sub-folders missing from the tree — Builder, Persona, Feature, Technical, Legal, Chat, Register, Archive — each seeded with a README deriving its purpose, the §2 `<type>-<version>-<date>.md` filename convention, and the §9 update cadence + confirmation gate.
- doc/ now holds all nine §1 sub-folders plus the Gia-WA-specific decisions/ (ADR) folder, which is intentionally retained though it is not part of §1.
- Time anchor re-fetched per TF-3 (prior anchor 15-05 was 7 days + multiple midnights stale): 22-05 '26 05:05 SGT. data.gov.sg air-temperature (sovereign — items[0].timestamp 2026-05-22T05:00+08:00) and the Google HTTP Date header (21-05 21:04:59 GMT) converge within ~5 min; the container `date` (21-05 22:10 SGT) was ~7 h adrift and discarded as the TF-9 sandbox-clock failure mode. worldtimeapi unreachable.
[RISK] Low. Documentation only — no .js touched, no dependency change, no secret handled, no runtime behaviour altered. The new folders are empty scaffolds carrying orientation READMEs.
[STATUS] ALPHA — still Phase 1 scaffold; no AI integration, no Cloud API smoke test.
[TEST]
- No source changed: node --check and vitest (27/27 from #001) remain valid; not re-run as no .js was modified.
- doc/ tree verified via `ls doc/`: Archive, Builder, Chat, Feature, Journal, Legal, Persona, Register, Technical, decisions all present.
[NEXT] Operator to decide: (a) commit this doc-system work — needs a serial-numbered commit message and bumps the commit counter; (b) whether to also create the lean doc/CLAUDE.md orchestrator (§17.5) and relocate CLAUDE-FULL.md → doc/CLAUDE-FULL.md per the §1 layout; (c) whether to extend doc/.serial-state.yml with the now-relevant builder/persona/feature/technical/legal/chat counters.
[ADVICE] Contract-grounded scaffolding — every README cites the specific CLAUDE-FULL.md rule (§1/§2/§9) it derives from, so the doc system stays auditable against the contract instead of drifting into ad-hoc structure.

---

(№ 3 - 22-05 '26 06:25 SGT)

[HDR] #003 | 06:25:00 SGT 22-05-26 | v0.1.0 | ~4kb | ~80 lines
[INTENT] Record the operator's 2026-05-22 revision of directive D-1 — Gemini is now permitted for the Hidden-Gems / R.E.D path — as a numbered ADR, and propagate the revision into BUILD-PLAN.md §2 and the repo-root CLAUDE.md so the constraint set stays consistent across all three authoritative docs.
[DELTA]
- doc/decisions/002-gemini-for-red-hidden-gems.md: new ADR-002. D-1 is partially revised — the main AI layer (classify / converse / stage-a / sanctuary) stays Llama-only via Groq (primary) / Together (fallback); Gemini is authorised SOLELY for the R.E.D ("Random Excellent Discovery") Hidden-Gems template path, via a plain Google AI Studio key (GEMINI_API_KEY), mirroring ang-kl/gia/gemini-client.js. The Anthropic bar in D-1 is untouched. ADR explicitly authorises the vendor but schedules no build.
- BUILD-PLAN.md §2: D-1 row marked [REVISED 2026-05-22 — see ADR-002]; row retained (not deleted) per the §2 revision rule; implication cell updated to scope Gemini to the R.E.D path only.
- CLAUDE.md: D-1 bullet updated to match the revised wording.
- doc/.serial-state.yml: decisions counter 1→2, commit counter 2→3, journal counter 2→3.
[RISK] Low. Documentation only — no .js touched, no dependency change, no secret handled, no runtime behaviour altered. ADR-002 authorises a vendor but no Gemini code exists yet; nothing can call Gemini until a phase is scheduled and built.
[STATUS] ALPHA — still Phase 1 scaffold; no AI integration, no Cloud API smoke test.
[TEST]
- No source changed: node --check and vitest (27/27 from #001) remain valid; not re-run as no .js was modified.
[NEXT] Operator to decide where the R.E.D / Hidden-Gems phase slots into BUILD-PLAN §12 — the current 0–7 plan has no such phase (§3.4 had deferred it). When built it needs: src/ai/gemini.js (sibling to llama.js), a Gemini R.E.D prompt under src/ai/prompts/, GEMINI_API_KEY added to .env.example + §9.1, and Gemini token/cost tracking in telemetry.js.
[ADVICE] ADR-002 separates authorisation from scheduling on purpose — the vendor is unblocked but no Gemini code lands until the operator slots the phase. This prevents speculative build of the ~150 KB-template R.E.D path that BUILD-PLAN §3.4 had explicitly deferred.

---

(№ 4 - 22-05 '26 06:25 SGT)

[HDR] #004 | 06:25:00 SGT 22-05-26 | v0.1.0 | ~1kb | ~6 lines
[INTENT] Unblock CI on PR #2. Both jobs (syntax, vitest) fail ~3s after start — before any test runs — which is a setup-stage failure, not a code defect: locally `npm install --ignore-scripts` + `npm run syntax` + `npm test` are all green (27/27).
[DELTA]
- .github/workflows/ci.yml: removed `cache: 'npm'` from the actions/setup-node@v4 step in both the `syntax` and `test` jobs. The npm cache is a pure optimisation; when the Actions cache service is unreachable or the lockfile path resolves oddly it can fail the setup-node step early — the only step common to both jobs that fails fast and identically. Removal is behaviour-neutral for the install/test result.
- doc/.serial-state.yml: commit 3→4, journal 3→4.
[RISK] Low. CI-config only — no source, no dependency, no runtime change. Worst case CI stays red and the next run log narrows the cause.
[STATUS] ALPHA — Phase 1 scaffold; CI-infra fix only.
[TEST] Local: npm install --ignore-scripts + npm run syntax (exit 0) + npm test (27/27 green). CI result pending the next run on PR #2.
[NEXT] If CI still fails after this, the cause is npm-registry / network egress from the runner — that needs the actual run log or an infra confirmation, not a further workflow edit.
[ADVICE] Single-variable change on purpose: green on the next run ⇒ the cache step was the culprit; still red ⇒ the failure is the npm-install network path and escalation (logs/infra) is required.

---

(№ 5 - 22-05 '26 06:25 SGT)

[HDR] #005 | 06:25:00 SGT 22-05-26 | v0.1.0 | ~3kb | ~40 lines
[INTENT] Schedule the operator-authorised (ADR-002) Hidden-Gems / R.E.D Gemini path into the BUILD-PLAN.md §12 phase plan. ADR-002 authorised the vendor but explicitly left the build unscheduled; this entry turns that into a concrete numbered phase.
[DELTA]
- BUILD-PLAN.md §12: inserted **Phase 5 — Hidden-Gems / R.E.D path (Gemini)** immediately after Phase 4 (Cuisine) — the phase it depends on. Renumbered the three following phases: Hawker 5→6, Telemetry/soak 6→7, Out-of-scope-decisions 7→8; branch slugs updated to match.
- BUILD-PLAN.md §12 hard-stop: now "operator approves before Phase 6 (Hawker)"; notes Phase 5 (R.E.D) is already ADR-002-authorised and may proceed once Phase 4 lands.
- BUILD-PLAN.md §13: added success-criteria row 5 (R.E.D vault-miss fallback); renumbered rows 5/6/7 → 6/7/8.
- BUILD-PLAN.md §6.1 tree: added src/ai/gemini.js and src/ai/prompts/red.js; hawker.json comment 5→6.
- BUILD-PLAN.md §7.2 router table: added the `hidden-gems` task row → Gemini / Google AI Studio.
- BUILD-PLAN.md §9.1: added GEMINI_API_KEY to the env-var list.
- BUILD-PLAN.md §10.3, §14, §16.4: phase-number cross-references corrected (5→6 where they referred to Hawker).
- doc/.serial-state.yml: commit 4→5, journal 4→5.
[RISK] Low. Documentation only — no .js touched, no dependency or runtime change. The renumber was verified by grep: every "Phase 5–8" reference now resolves to the intended phase. No prior journal HDR (#001–#004) references Phase 5–7 by number, so the renumber does not corrupt the append-only history.
[STATUS] ALPHA — still Phase 1 scaffold; this only schedules future work.
[TEST] No source changed: node --check and vitest (27/27 from #001) remain valid; not re-run as no .js was modified.
[NEXT] Phase 2 (Llama wrapper + router) remains the next build phase and is unchanged. The Phase 5 (R.E.D) build needs GEMINI_API_KEY provisioned and Phase 4 landed first.
[ADVICE] R.E.D was slotted at Phase 5 — not appended after the Phase 8 docs-wrap — because it depends only on Phase 4's search path; numbering it last would imply a false execution order. Integer renumbering was preferred over a fractional "Phase 4.5" to keep the §12 / §13 / branch-slug sequence clean and greppable.

---

(№ 6 - 22-05 '26 06:25 SGT)

[HDR] #006 | 06:25:00 SGT 22-05-26 | v0.1.0 | ~5kb | ~110 lines
[INTENT] Capture a self-contained handoff runbook for the next session to wire the BUILD-PLAN §8.1 read-only bridge to ang-kl/gia. This session is access-scoped to ang-kl/gia-wa only — both the GitHub MCP allowlist and the local git proxy refuse ang-kl/gia ("Proxy error: repository not authorized") — so the bridge cannot be set up here; it needs an environment reconfigured to authorize ang-kl/gia read-only.
[DELTA]
- doc/bridge-setup-plan.md: new one-off handoff runbook. Sections A–F: operator reconfigures the environment → new session verifies gia is reachable → resolves the submodule-URL-stability gotcha (the proxy port changes per container, so .gitmodules cannot hard-code a proxy URL) → adds gia as a shallow/sparse submodule under vendor/soleat/ → logs ADR-003 (bridge ahead of Phase 4; confirms §8.1 Option B; resolves the §14 vault-bridge open question) → audits gia's substantial changes vs the §3 audit. Includes a copy-paste kickoff prompt. Placed at doc/ root (not a §1 subfolder, same rationale as decisions/).
- doc/.serial-state.yml: commit 5→6, journal 5→6.
[RISK] Low. Documentation only — no .js, no dependency, no runtime change. The runbook is advisory; nothing executes until a reconfigured environment exists.
[STATUS] ALPHA — Phase 1 scaffold; the bridge itself is not yet built (blocked on environment access).
[TEST] No source changed: node --check and vitest (27/27 from #001) remain valid; not re-run.
[NEXT] Operator reconfigures the environment to authorize ang-kl/gia read-only, then starts a new session and runs doc/bridge-setup-plan.md. On bridge-PR merge, archive this runbook to doc/Archive/ per Rule N-3.
[ADVICE] The bridge blocker is an environment access boundary, not a code problem — do not attempt workarounds (direct github.com clone fails: no credentials; the local proxy denies gia by policy). The only correct fix is reconfiguring the environment's authorized-repo set.

---

(№ 7 - 22-05 '26 06:25 SGT)

[HDR] #007 | 06:25:00 SGT 22-05-26 | v0.1.0 | ~6kb | ~55 lines
[INTENT] Wire the BUILD-PLAN §8.1 read-only data bridge to ang-kl/gia. The operator made ang-kl/gia public, which made it reachable via canonical https without the (gia-denying) proxy or credentials — so the bridge could be set up directly from this session rather than the deferred handoff.
[DELTA]
- vendor/soleat: ang-kl/gia added as a git submodule (§8.1 Option B), url https://github.com/ang-kl/gia.git, pinned at v0.61.99 (commit ddf5f37, HEAD dated 2026-05-22). Cloned --depth 1 with GIT_LFS_SKIP_SMUDGE=1.
- .gitmodules: created.
- package.json: `syntax` script now also excludes ./vendor/* — without it, node --check would run over all ~85 of gia's root .js files.
- vitest.config.js: new file — excludes vendor/** so Soleat's own test suite (needs @anthropic-ai/sdk + gia modules; fails) does not run in Gia-WA's vitest.
- doc/decisions/003-gia-bridge-submodule.md: ADR-003 — confirms §8.1 Option B, logs the deviation (bridge wired at Phase 1, not Phase 4), records the 920MB / 16-vault-version finding, the sparse-checkout recommendation, and the tooling isolation.
- BUILD-PLAN.md §8.1: added a "Status — wired" note; §14: vault-bridge open question marked resolved.
- doc/.serial-state.yml: commit 6→7, journal 6→7, decisions 2→3.
[RISK] Low–moderate. The submodule gitlink does not bloat Gia-WA's repo; the cost is the ~920MB pull for anyone running `git submodule update --init` (mitigation: the ADR-003 sparse-checkout recommendation). D-2 intact — vendor/soleat is read-only, never edited or PR'd. Preflight re-verified green after the tooling-isolation fixes.
[STATUS] ALPHA — Phase 1 scaffold; the bridge is wired but the src/data/ loaders remain deferred to Phase 4.
[TEST] npm run syntax → clean (vendor/ excluded). npm test → 27/27 (vendor/ excluded; without the exclude, vitest pulls in gia's failing suite — verified).
[NEXT] Audit gia v0.61.99 against BUILD-PLAN §3's ~2026-05-15 audit — what changed, which §3a/§3.1/§3.2/§3.3 findings are now stale. Phase 4 wires src/data/vault.js + cuisines.js against vendor/soleat/ with sparse-checkout.
[ADVICE] gia is a live production repo (HEAD dated today). Keep the submodule pinned to an explicit commit and bump it only on a deliberate re-sync — never let it track gia's moving HEAD, or Gia-WA's data drifts silently mid-development.

---

(№ 8 - 22-05 '26 06:25 SGT)

[HDR] #008 | 06:25:00 SGT 22-05-26 | v0.1.0 | ~5kb | ~50 lines
[INTENT] Audit ang-kl/gia v0.61.99 (now readable via the §8.1 bridge) against BUILD-PLAN §3's ~2026-05-15 source-project audit, and apply the resulting factual corrections to BUILD-PLAN.md.
[DELTA]
- doc/gia-audit-2026-05-22.md: new — full audit report. Headline: BUILD-PLAN §3 holds up; gia is intensely active (~306 commits, 4 releases on the audit day) but architecturally stable. §3.1 (Telegram surface) accurate; §3.2 (AI integration) accurate — llm-client.js is the single Claude integration point, gemini-retry.js is a misleadingly-NAMED generic retry helper (main layer is Claude); §3a (engine inventory) accurate (84 root .js vs ~85). §3.3 was stale.
- BUILD-PLAN.md §3.3: rewritten — there is no data/cuisines.js; the catalogue is cuisines-vault.js at the gia root (source of truth doc/Feature/cuisines_js.MD), and each vault/<version>/ is a full repo snapshot (→ 16 versions = 766MB).
- BUILD-PLAN.md §8.1: the data/cuisines.js path corrected to cuisines-vault.js in Options A, B and C, and in the ADR-003 "wired" note's sparse-checkout target.
- BUILD-PLAN.md §3.4: the "Gemini R.E.D path — not ported" bullet marked [REVISED — ADR-002]; it is now in scope as Phase 5.
- doc/.serial-state.yml: commit 7→8, journal 7→8.
[RISK] Low. Documentation only — no .js, no dependency, no runtime change. Corrections are factual (verified against vendor/soleat at gia v0.61.99). D-2 intact — the audit was read-only.
[STATUS] ALPHA — Phase 1 scaffold; planning-doc accuracy work.
[TEST] No source changed: node --check and vitest (27/27) remain valid; not re-run.
[NEXT] Side-finding flagged for the operator: the submodule brings vendor/soleat/CLAUDE.md into the tree — Gia-WA's root CLAUDE.md should note vendor/ is the read-only submodule so a future session does not mistake gia's doc contract for nested instructions. Phase 2 (Llama wrapper) remains the next build phase.
[ADVICE] The first read of gia's AI layer was misleading (gemini-retry.js imports looked like a Gemini main layer); verification against llm-client.js corrected it. Audit findings that change a plan must be verified at the source before they are written down.

---

(№ 9 - 29-05 '26 15:10 SGT)

[HDR] #009 | 15:10:00 SGT 29-05-26 | v0.1.0 | ~14kb | ~490 lines
[INTENT] Build Phase 2 (BUILD-PLAN §12): Llama AI layer — router, chat wrapper, dedup cache, telemetry — against mocks so the full suite runs without live API keys. Open draft PR #7.
[DELTA]
- src/ai/router.js: pure pick(task) → {provider, model, temperature, response_format}. Covers classify/converse/stage-a/sanctuary/vision per §7.2 model table.
- src/ai/llama.js: OpenAI-compatible chat({task, messages, route, tools}). Groq primary (3 s timeout) → Together AI fallback on 429 / 5xx / network timeout. Model-ID remapping for Together (TOGETHER_MODEL_MAP). Per-call telemetry via record().
- src/ai/dedup.js: makeDedup(redis) factory — 60 s (lang, sha256(text)) → response cache. wa:dedup: keyspace.
- src/ai/telemetry.js: init(redis) + record() + dailySummary(). Appends per-call entry to wa:telemetry:calls (capped at 10 000). Estimates cost_usd from COST_PER_1M table. No-op when redis is null.
- src/ai/prompts/classifier.js: classifier system prompt with a 10-entry stub cuisine catalogue. TODO(§8.1-bridge) marks the swap-in point for the real gia/data/cuisines.js once the §8.1 bridge lands in Phase 4.
- __tests__/ai/router.test.js (7), llama.test.js (8), dedup.test.js (5), telemetry.test.js (5): 25 new tests, all against mocks/fakes — no live keys required.
- doc/.serial-state.yml: commit counter 8→9, time anchor refreshed to 29-05 '26 15:10 SGT (NEA data.gov.sg).
[RISK] Low. No live integrations; D-1 upheld (no @anthropic-ai/sdk). Classifier prompt uses a stub catalogue — Phase 3 smoke will surface any shape mismatches before merge.
[STATUS] ALPHA — Phase 2 AI layer built and tested in mock mode. Phase 3 (inbound text path) is next; blocked on GROQ_API_KEY + TOGETHER_API_KEY for the live §13 smoke criterion.
[TEST]
- npm run syntax → 0 errors
- npm test → 52/52 green (25 new + 27 from Phase 1)
[NEXT] Operator to provision GROQ_API_KEY + TOGETHER_API_KEY, then run live classifier smoke (§13 Phase 3 criterion: ≥90% venue-intent accuracy on 50-message set). Once keys are in .env, Phase 3 branch (claude/phase-3-text) can open: onMessage.js + sendButtons/sendList/sendLocationRequest stubs.
[ADVICE] The makeDedup(redis) factory pattern (dependency injection) is the right shape for the whole AI layer — keeps tests fast and free of I/O without module-level mocking gymnastics. Apply the same pattern to any future module that needs Redis.

---

(№ 9 - 29-05 '26 15:10 SGT)

[HDR] #010 | 15:10:00 SGT 29-05-26 | v0.1.0 | ~14kb | ~520 lines
[INTENT] Post-merge record for PR #7 (D-4 standing rule).
[DELTA] PR #7 merged to main at sha 89675265ac007a1746534417903dbeda1c94c733. No additional file changes — merge commit only.
[RISK] None. Merge was a fast-forward-compatible merge commit; no conflicts.
[STATUS] ALPHA — Phase 2 on main. Phase 3 (inbound text path) is the next build phase.
[TEST] 52/52 green on branch pre-merge; no changes in merge commit.
[NEXT] Provision GROQ_API_KEY + TOGETHER_API_KEY → live classifier smoke → open claude/phase-3-text.
